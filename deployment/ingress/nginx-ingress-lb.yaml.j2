apiVersion: v1
kind: Service
metadata:
  name: nginx-ingress-lb
  labels:
    app: nginx-ingress
spec:
  type: NodePort
  externalIPs:
  - {{ ingress.external_ip }}
  ports:
  - port: 80
    name: http
    nodePort: {{ ingress.node_port }}
  selector:
    app: nginx-ingress


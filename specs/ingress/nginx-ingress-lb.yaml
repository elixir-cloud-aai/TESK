apiVersion: v1
kind: Service
metadata:
  name: nginx-ingress-lb
  labels:
    app: nginx-ingress
spec:
  type: NodePort
  externalIPs:
  - 172.16.1.1
  ports:
  - port: 80
    name: http
    nodePort: 30977
  selector:
    app: nginx-ingress
